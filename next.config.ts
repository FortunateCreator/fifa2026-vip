import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  devIndicators: false,
  allowedDevOrigins: ['187.127.229.131', 'reached-necessity-administrators-roommates.trycloudflare.com', 'vantage26.com', 'www.vantage26.com'],
};

export default nextConfig;
